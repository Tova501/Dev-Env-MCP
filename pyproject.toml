[project]
name = "dev-env-mcp"
version = "0.1.0"
description = "Safe MCP server for dev environment introspection"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.25.0",
    "psutil>=7.2.1",
]
